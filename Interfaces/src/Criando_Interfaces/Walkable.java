package Criando_Interfaces;

public interface Walkable {

    // Atributos em Interfaces são public, static e final

    void walk(); // Métodos em interfaces são abstracts e public obrigatoriamente
    void stop();
}
