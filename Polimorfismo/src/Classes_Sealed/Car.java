package Classes_Sealed;

public non-sealed class Car extends Vehicle { // Do lado da classe que vai extender a classe sealed é necessário um modificador
                                              // final (não permite herança), non-sealed (permite herança), sealed (herança controlada)
}
