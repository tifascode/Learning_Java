package Metacaracteres;

public class App {
    public static void main(String[] args) {

        // META CARACTERES
        // -> /d (digitos 0-9)
        // -> /s (espaço em branco)
        // -> /w (caracteres alfa-numericos: digitos, letras, "_")
        // -> .  (qualquer caractere)
        // -> [abc] (ou "a" ou "b" ou "c")
        // -> [a-f] (qualquer caractere de "a" até "f")
        // -> [a-f0-9] (qualquer caractere de "a" até "f" ou de "0" até "9")

        // QUANTIFICADORES
        // -> * (zero ou mais ocorrências)
        // -> + (Uma ou mais ocorrências)
        // -> ? (nenhuma ou uma ocorrência)

    }
}
